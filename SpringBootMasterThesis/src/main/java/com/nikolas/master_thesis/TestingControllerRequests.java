package com.nikolas.master_thesis;

//@Component
public class TestingControllerRequests {
/*
	// TODO: Research HOW TO write AUTOMATED tests for Multithreaded requests!
	final AuthorController authorController;
	

	@Autowired
	public TestingControllerRequests(AuthorController authorController) {
		this.authorController = authorController;
	}

	
	@PostConstruct
	public static void test(){
		int NUMBER_OF_THREADS = 1000;
		ExecutorService executor = Executors.newFixedThreadPool(NUMBER_OF_THREADS);		
		
		Callable<ResponseEntity<List<Author>>> callable = () -> {
//			authorController.getAllAuthors(); // SOLVE this!!!
			return null; 
		};
		
		executor.submit(() -> {
			for (int i = 0; i < NUMBER_OF_THREADS; i++) {
				
			}
		});

	}// test
*/
}
